"""
Shared OpenRouter call wrapper (requests + tenacity + JSON parsing).

Placeholder skeleton (Commit 1). The wrapper currently duplicated in each
agent's classifier.py will be extracted here in the "Extract shared" commit.
"""
from __future__ import annotations
