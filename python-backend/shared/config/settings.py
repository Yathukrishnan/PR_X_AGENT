"""
Shared settings / env loading.

Placeholder skeleton (Commit 1). The contents of the existing
config/settings.py will be moved here in the "Extract shared" commit, and all
`from config.settings import ...` callers repointed to `shared.config.settings`.
"""
from __future__ import annotations
